/* const imprimirInformacoesPessoa = (primeroNome, ultimoNome, idade) => {
    return "Primeiro Nome: " + primeroNome + ", Último Nome: " + ultimoNome + ", Idade:" + idade;
}

console.log(obterInformacoesPessoa ("Roberto", "Dias", 33)) */

const imprimirInformacoesPessoa = (primeroNome, ultimoNome, idade) => {
    return `Primeiro Nome: ${primeroNome}, Último Nome: ${ultimoNome}, Idade: ${idade}`;
}

/* console.log(obterInformacoesPessoa ("Roberto", "Dias", 33)) */
console.log(`${imprimirInformacoesPessoa("Roberto", "Dias", 33)}, ele é um DevQuester`)

console.log(`Soma ${10 + 20}`)